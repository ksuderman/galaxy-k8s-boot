apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ebs
provisioner: "{{ csi_driver | trim }}"
volumeBindingMode: {{ pv_binding_mode | default('WaitForFirstConsumer') }}
parameters:
  type: "{{ sc_parameters.type | trim }}"
