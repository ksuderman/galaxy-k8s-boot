---
# External Ansible roles and collections required for galaxy-k8s-boot

collections:
  - name: community.general
    version: ">=11.3.0,<12.0.0"
  - name: ansible.posix
    version: ">=2.1.0,<3.0.0"
  - name: kubernetes.core
    version: ">=6.4.0,<7.0.0"

roles:
  - name: galaxyproject.cvmfs
    version: "0.5.0"
