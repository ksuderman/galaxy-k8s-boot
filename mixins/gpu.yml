# GalaxyAI configuration for the self-hosted GPU backend (ai_backend=ollama-gpu):
# Ollama running qwen2.5:7b on an NVIDIA GPU behind LiteLLM.
# The dev image is required for the latest GalaxyAI.
image:
  repository: quay.io/galaxyproject/galaxy-min
  tag: "dev"

# On a GPU the 7b model reliably produces the tool-call / JSON structured output
# the GalaxyAI agents expect, so the CPU band-aids are left commented out below
# (structured_output_override, and disabling custom_tool) -- unlike mixins/cpu.yml,
# which needs them for the weaker CPU 3b model. Only a small retry budget is kept.
#
# The runtime ai_api_base_url / ai_model / ai_api_key are injected by the
# playbook (they depend on the in-cluster LiteLLM service + generated key), so
# this mixin only carries the static agent tuning.
configs:
  galaxy.yml:
    galaxy:
      logo_url: null
      brand: Ollama-GPU
      inference_services:
        default:
          # Parse plain-text responses instead of demanding structured/tool-call
          # output the small CPU model can't reliably produce. Avoids the
          # validation-retry death loop that returns the fallback message.
          # structured_output_override: false
          # Fail fast instead of burning ~40-50s x 3 on doomed retries.
          retries: 1
        custom_tool:
          # The custom-tool agent (code generation + its own reflection loop) is
          # the most expensive; disable it to cut per-question fan-out.
          # enabled: false

extraFileMappings:
  /galaxy/server/static/welcome.html:
    applyToWeb: true
    applyToNginx: true
    content: |
      <!DOCTYPE html>
      <html lang="en">
          <head>
              <meta charset="utf-8" />
              <link rel="stylesheet" href="dist/base.css" type="text/css" />
          </head>
          <body class="m-0">
              <div class="py-4">
                  <div class="container">
                      <div class="row mb-4">
                          <div class="col-md-12">
                              <h2>Welcome to Galaxy</h2>
                              <p>
                                  Galaxy is an interactive and workflow analysis
                                  platform providing a graphical user interface
                                  for computational tools. This is your own
                                  Galaxy instance that you can customize and use
                                  for your own research.
                              </p>
                              <h2>GalaxyAI</h2>
                              <p>
                                This Galaxy instance has been configure to route 
                                GalaxyAI to a self-hosted Ollama on a GPU server 
                                behind LiteLLM, running qwen2.5:7b.
                              </p>
                          </div>
                      </div>
                  </div>
              </div>
          </body>
      </html>

