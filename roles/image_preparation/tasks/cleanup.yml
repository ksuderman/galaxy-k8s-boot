---
- name: Clean package cache
  ansible.builtin.apt:
    autoclean: true
    autoremove: true
  become: true

- name: Remove temporary files
  ansible.builtin.shell:
    cmd: |
      # Exclude .ansible* to avoid deleting Ansible's own temp files mid-task
      find /tmp -mindepth 1 -maxdepth 1 -not -name '.ansible*' -not -name 'ansible_*' -exec rm -rf {} +
      find /var/tmp -mindepth 1 -maxdepth 1 -not -name '.ansible*' -not -name 'ansible_*' -exec rm -rf {} +
      rm -f /var/cache/apt/archives/*.deb
  ignore_errors: true
  changed_when: false
  become: true

- name: Reset history
  ansible.builtin.copy:
    content: |
      tail -n +1 -f /var/log/cloud-init-output.log
      kubectl config view --raw
    dest: "/home/{{ default_user }}/.bash_history"
    mode: "0600"
    owner: "{{ default_user }}"
    group: "{{ default_user }}"
  ignore_errors: true

- name: Clear root bash history
  ansible.builtin.file:
    path: /root/.bash_history
    state: absent
  ignore_errors: true
  become: true

- name: Clear log files
  ansible.builtin.shell: |
    find /var/log -type f -name "*.log" -exec truncate -s 0 {} \;
    find /var/log -type f -name "*.log.*" -delete
  become: true
  changed_when: false

- name: Clear machine-id to ensure unique instances
  ansible.builtin.copy:
    content: ""
    dest: /etc/machine-id
    mode: "0644"
  become: true

- name: Clear cloud-init cache
  ansible.builtin.shell: rm -rf /var/lib/cloud
  become: true
  ignore_errors: true

- name: Remove SSH keys (will be regenerated on first boot)
  ansible.builtin.shell: |
    rm -f /etc/ssh/ssh_host_*
    rm -f /home/{{ default_user }}/.ssh/authorized_keys
  become: true
